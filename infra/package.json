{
  "name": "dax-demo-infra",
  "version": "1.0.0",
  "private": true,
  "description": "CDK app: VPC + DAX cluster + DynamoDB table + EC2 runner for the DAX REST demo.",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "build": "tsc"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.140.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "aws-cdk": "^2.140.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
